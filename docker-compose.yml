services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spenzly
    volumes:
      - spenzly_pgdata:/var/lib/postgresql/data

volumes:
  spenzly_pgdata:
